backbone: "CLIP-ViT-B/16"
resolution: 224
num_shots: 16

output_dir: null
print_freq: 10

seed: 0
deterministic: True
gpu: 0
num_workers: 8
prec: "amp"

continue_train: True
continue_train_first: False
num_epochs: 20
batch_size: 64
lr: 0.001
weight_decay: 5e-4
loss_type: "CE"
lada_k: 16      # lambda_1 in paper
prototype_k: 4  # lambda_2 in paper
image_prototypes_weight_coef: 64.0

t_adaptformer: True
t_adapter_dim: 16
